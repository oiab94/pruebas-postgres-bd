-- Permite el orden en el que se presentan las tuplas de una relación
SELECT 		attribute_1, attribute_2
FROM 			table_1
ORDER BY 	attribute_2 ASC

-- Las direcciones ASC o DESC permite controlar el orden en que se puede presentar
-- el campo attribute_2