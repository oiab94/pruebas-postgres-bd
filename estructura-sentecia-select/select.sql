SELECT 		a1, ...,an			-- Describe la salida deseado con:
													-- Nombres de columnas, expresiones aritméticas, literales
													-- funciones escaleras, funciones de columna

FROM 			t1, ..., tn			-- Nomnbre de tablas / vistas

WHERE			P								-- Condiciones de selección de filas

GROUP BY 	ai1, ..., ain		-- Nombre de las columnas

HAVING 		q								-- Condiciones de selección de grupo

ORDER BY 	aj1, ..., ajn		-- Nombres de columnas